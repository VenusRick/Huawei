# 测试报告与部署使用手册

## 历史回归记录（非当前验收汇总）
- 检视整改标尺 tests/regression/test_review_regressions.py：41 用例
  （原 31 + GREASE 3 + W4 4 + §5.1 3）
- 契约层 tests/test_contract_services.py：17 用例
- 行为上下文 tests/test_behavior_context.py：10 用例
- 存量 tests/test_all.py：6 用例
- 一键验收 scripts/run_acceptance.py：G01-G05 全 PASS
  （output/acceptance_p0_004/acceptance.json）

## 端到端实测
- 应用识别（合成 fixture）：6/6 会话全对，acc/macro-F1=1.0，FPR=0
- 行为识别（合成 fixture_m2）：行为窗 32/32（1.000）；背景边缘窗误报
  为已知边界（训练无 background 负类）
- 人工修正：禁用冗余规则回放 0 变化（真实结果）；非法编辑明确拒绝
- 真实数据（CSTNET-TLS1.3，分组口径）：
  **握手可见五类**（huawei/overleaf/twimg/acm/vivo）：acc 0.874 /
  macro-F1 0.897（拒识计入完整分母，剔除无握手病类文件后重算）
  **无握手段**（gitlab 类，三文件 0% TLS 握手）：类间可分性不足，
  规则保守拒识（召回 0/119，未虚标）；全量混合口径 acc 0.705/F1 0.742/
  FPR 0.9% 留档对照。归因与 A 实验（换类反证类互扰）见 M4 进展文档
  第七/十一节
- 性能实测：output/performance.json（解析63MB/s、匹配0.01ms/会话）

## 部署
本轮验证环境：Python 3.10.12；挖掘依赖、pandas、numpy、scipy、scikit-learn、xgboost（仅挖掘端）；
推理端仅需运行时代码 + bundle。安装后按 工具技术说明书.md 命令执行。
## 已知边界
真实行为数据不足 / report.html 最小化 / 长连接与高并发资源表现尚未全量测量 / QUIC 解保护未实现
（报可见范围）。

## 2026-09-18 第二轮当前验证结果

以下记录对应当前增量实现。此前本文件中的G09/五类分组/性能数值保留为历史诊断，
其代码和输入不同，不能作为当前版本成绩或赛题达标依据。

- 主机全套测试：145 passed，2个既有scipy常量输入警告；未修改既有122项断言。
- CSTNET网站前2000包小批次：train41/validation88/test10；Accuracy10.00%、Macro-Precision8.33%、Macro-Recall16.67%、Macro-F1 11.11%、Macro-FPR1.85%、拒识8/10。各类FPR最高11.11%，不能用宏平均替代背景误报。
- 合成行为g4测试：37窗，Accuracy86.49%、Macro-Recall84.38%、Macro-Precision100%、Macro-F1 90.74%；背景0/5误报，事件TP2/FP0/FN0。仅两个合成行为事件，不代表真实业务达标。
- 真实2000包/31会话PCAP、交错新进程3次：全算CLI中位数2.4342s，规则依赖按需0.2439s（9.98×），输出逐条一致。这是单文件端到端CLI测量，不是网络线速。
- 子进程封锁sklearn/xgboost/scipy，正式app与behavior CLI均无导入尝试，预测复现。
- 自动有效率18/319=5.64%为内部严格代理口径，低于80%目标；人工修正可用率90%缺人工记录，真实规定场景和Android/iOS/PC证据仍缺。
- 证据：output/chain_improve_20260918_02/host_verify/；报告：docs/实施结果/全链增量改进_20260918_02.md。
