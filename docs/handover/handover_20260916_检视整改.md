# 交接文档：检视问题整改（2026-09-16 会话二）

## 背景与输入
- 用户提供外部检视三件套：`docs/实施方案/01_代码检视报告.md`（A01-A16）、`02_Agent执行与验收方案.md`、`review_results.json`（33 回归用例：31 fail / 2 pass）
- 检视方用例文件 `regression/test_review_regressions.py` 不在本仓库，本次以"逐项合成验证"替代，S0 的 pytest 化标尺未建（后续建议按 01 报告重建）

## 已修复并验证（14/31，均带合成验证 + test_all 6/6 零回归）

### S1 解析层（R01-R09）
| R# | 病根 | 修复 | 文件 |
|---|---|---|---|
| R01 | `_create_session` 用 canonical 字典序端点当发起方 | 会话端点=首包真实发起方 | session_manager.py `_create_session` |
| R02 | SYN/SYN-ACK 的 expected_seq 写死 fwd/bwd 键不看方向 | 按包自身 is_fwd 写键；零载荷包不推进基准 | `_process_tcp_packet`/`_check_reorder_retransmit` |
| R03 | 乱序包 buffer 后又被外层 append（双计） | 外层跳过 is_out_of_order；flush_all 按 seq 补入残留缓冲并清 tracker | 同上 + `flush_all` |
| R04 | UDP 无 ingest 超时 | process_packet 开头对同五元组旧会话做空闲超时关闭 | `process_packet` |
| R05 | FIN 不回收会话 | tracker 加 fin_fwd/fin_bwd，双向 FIN→CLOSED+回收 | `_process_tcp_packet` |
| R06 | duration 的 `if start_time` truthy 陷阱（0.0 起点） | 直接 end-start | FlowSession.duration |
| R07 | magic 白名单缺纳秒 0xa1b23c4d/0x4d3cb2a1；tsresol 判定查错字段(data[8:12]) | 白名单补全+magic 直接定精度 | pcap_reader.py `_read_packets` |
| R08 | pcapng 全按小端解析，大端 section 静默读空 | SHB BOM 判 section 字节序，全字段 endian 化，无效 BOM raise | `_read_pcapng` 重写 |
| R09 | TCP/UDP payload 切到帧尾，以太网 padding 入载荷 | 按 IP total_length 截断 | `_process_raw_packet` |

### S2 协议层（R10-R14）
| R# | 病根 | 修复 |
|---|---|---|
| R10 | 短头返回 dict 被 `.is_valid` 访问 AttributeError 炸整包 | isinstance 防护 + pcap 侧 QUIC try/except 降级 |
| R11 | varint 掩码链缺 8 字节档 | 补 `0x3FFFFFFFFFFFFFFF` |
| R12 | TLS 只解析单包完整 record，跨分段 ClientHello 丢 SNI | pcap_reader 新增 `_tls_feed` 按方向缓冲重组 |
| R13 | 截断 record 被当完整 | parse_record 校验 record_length 凑满 |
| R14 | JA3 不滤 GREASE | `_is_grease`（&0x0F0F==0x0A0A）滤 ciphers/exts/curves/formats + ja3s |

### S3 特征层（R15/R17）
| R# | 病根 | 修复 |
|---|---|---|
| R15 | 字节频数数组被 `_compute_entropy` 再 bincount（均匀分布熵=0 非 8） | 调用处按频数直接算熵 |
| R17 | burst 二级聚合 zip(burst_words, packets) 长度错位、时间语义丢失 | `_aggregate_to_bursts` 返回 (word,窗口序号)，新增 `_aggregate_bursts_to_behavior` 按窗口时间分组 |

## 未修复清单（17 项，根因已侦察定位）
- **R16** schema 随包数变化：短流/长流分支特征键集不一致（`feature_extractor.py:204+`、`advanced_extractor.py:60+`）。修法：统一 schema 层（短流补全键）
- **R22** 缺失必需特征被静默零填、**R24** 特征提取污染原 session（payload 被原地改）、**R30** 截断前缀时长用观测前缀（advanced_extractor 60-132 区间）
- **R18** 树路径未要求全条件 AND、**R19** not_exists 不匹配缺失值、**R20** 规则输出不遵守验收阈值、**R21** class 0 不导出、**R31** 叶标签不用 classes 映射 → `optimized_engine.py:76-213`、`optimized_generator.py:257-303`
- **R27** val/test 标签重编号（`test_generic_pipeline.py:205-215,318-335`）
- **R23** 保存的 DPI 标签不做拒识、**R29** 阈值搜索不计被拒 known 样本（`test_generic_pipeline.py:460-489,746-787,1054-1073`）
- **R25/R28** mine 入口空输入生成 demo 成功 + 调用不存在的 generate_rules（`pipeline.py:85-104,127-168`，A01 P0 第一优先）
- **R26** 配置 yaml 不可靠往返（config.py from_yaml/to_yaml）

## 验证状态
- 逐项合成验证：14/14 PASS（每层验证脚本见会话记录）
- `python tests/test_all.py`：6/6（S1 后、S2 后各验一次）
- sample_debug 全链路：会话尾部启动（output/sample_debug_run.log），结果见进度.md 后续更新

## 下一步建议（按 02 方案优先级）
1. P0：修 A01（R25/R28）——主入口失败语义 + demo 独立命令
2. 重建 `tests/regression/test_review_regressions.py`（按 01 报告 R 清单，本次合成验证可作蓝本）
3. R16 schema 统一（02 方案 6.3 FeatureRegistry 的最小版）
4. 规则引擎语义（R18-R21/R31）与标签重编号（R27）
5. 修完逐层跑 33 用例标尺 → 三态验收表
