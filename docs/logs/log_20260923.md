# 工作日志 2026-09-23

## 本次工作内容

- 将 /workspace/Huawei 项目首次推送到 GitHub 私有仓库 `VenusRick/Huawei`
  （此前本地无 remote，GitHub 上也无对应仓库，本次为新建）

## 修改的文件及原因

- 无代码改动；仅 git 层操作

## 遇到的问题与解决方案

- `git` 报 dubious ownership：已 `git config --global --add safe.directory /workspace/Huawei`
- 体积审计：`output/`（1.3G）已被 .gitignore 排除；`data/all_data` 与
  `data/sample_debug/*.pcap` 均为软链（git 只记路径），data/task 下 jsonl 全部 KB 级，
  无 pcap 实体入库风险

## git 操作记录

- 在 `recovery/claude-20260918` 分支提交 89 个文件（+19815/-732），commit `db3aaa8`
  内容：全链路优化、快速特征体系、全场景冲刺、扩类60、多数据集混合实验、
  行为/协议/隧道验收、跨平台闭环、实验裁决报告
- `gh repo create VenusRick/Huawei --private` 新建远端
- 推送 `master`（2f7b68b 基线）与 `recovery/claude-20260918`（db3aaa8）两分支，
  一次成功，未触发 GnuTLS -54（体量仅 ~10MB 文本）

## 复现命令

```bash
git remote add origin https://github.com/VenusRick/Huawei.git
git push -u origin master recovery/claude-20260918
```

## 下一步建议

- 后续增量更新直接 `git push` 即可；若单次变更超 ~50MB，参考 MCMP 推送经验
  （拆单文件 commit 逐批推 + Clash 18080 代理交替重试）
- 是否将默认分支从 master 切到 recovery/claude-20260918（最新工作状态）待定

## 追加：仓库转公开 + 默认分支切换

- 敏感扫描：grep 密钥关键词命中均为误报（QUIC 协议 token / TLS 扩展常量 / 数据集文件名），
  docs/资料/ 下仅公开论文 PDF，无真实凭证或内部材料
- `gh api PATCH` 将仓库改为 public；默认分支从 master 切到 recovery/claude-20260918
- 本节由本机 gh CLI 不支持 --accept-visibility-change-consequences 标志，改走 API 实现
