# M1 阶段进展（2026-09-17）

> 输入：《01_全流程贯通与赛题交付_增量实施方案.md》
> 前情：2026-09-16 已修 16 项（见 `整改实施结果_20260916.md`）
> 本文档只记录实际落盘并验证的事实。

## 一、本会话成果

### 回归标尺（S1）
- 方案所称 `reference_regressions/` 未随包落仓库（全库零命中）→ 按检视报告自建
- 新建 `tests/regression/test_review_regressions.py`：**28 个用例**（2 控制 + 16 A 档移植 + R16/R24/R25/R26 + C 档 R18/R19/R20/R21/R22/R31）
- 待补 4 项：R23/R27/R29（评价层接口随 S2 服务化稳定后写）、R30（advanced 截断前缀）
- **当前 28/28 全绿**；`tests/test_all.py` 6/6 全绿（PYEXIT=0）

### 新修复 10 项（累计 26/31）
| R# | 病根 | 修复 | 文件 |
|---|---|---|---|
| R16 | 单包流在 extract_all 开头提前 return，burst 等后续段整族缺键；短流兜底 dir_2gram 8 键 vs 长流 4 键 | 去掉早退（空序列已有零值兜底）；键数统一为 4 | feature_extractor.py / advanced_extractor.py |
| R18 | `_evaluate_rule` 按命中比例软化（score≥0.75 即命中） | 严格 AND：score==1.0 才匹配 | optimized_engine.py |
| R19 | `not_exists` 把值 0 当缺失 | 仅空值成立；0 是合法观测 | 同上 |
| R20 | 树导出硬编码 `confidence >= 0.7` | 用 `self.confidence_threshold` 配置 | optimized_generator.py |
| R21 | `predicted != 0` 硬编码丢弃 class 0 | 0 类同等导出 | 同上 |
| R22 | 规则所需特征缺失被 `continue` 静默跳过 | 除 not_exists 外缺失→整条规则不匹配 | optimized_engine.py |
| R25 | mine 空输入/无会话/无标签静默降级演示成功 | 三处改为 raise（FileNotFoundError/ValueError）；demo 经显式 `allow_demo=True`；tests/test_all.py 调用点同步显式化 | pipeline.py / test_all.py |
| R26 | to_yaml 把 Path dump 成 python/object 标签，from_yaml 读回即炸 | 路径字段序列化为字符串；from_yaml 按类型天然忽略（保持自动定位） | config.py |
| R28 | mine 调用不存在的 `generate_rules` | 改调真实 API `fit_and_generate`（参数天然对位） | pipeline.py |
| R31 | 叶标签用 `np.argmax` 索引当类别 ID（字符串标签错位为 class_N） | `_traverse_tree` 接收 `tree.classes_`，按真实类别值解析 | optimized_generator.py |
| A01附 | `session._source_file` 解析后未设置，标签无法按源文件回溯（labels.get(None,0) 全 0） | 解析循环内显式设置 | pipeline.py |

### 标尺的实战价值（如实记录）
C 档六用例首次运行全部失败，暴露上一轮"PATCHED"补丁实际未落盘（锚点空行差异致 assert 提前退出）——**标尺当场抓住静默失败**，修正后复绿。本轮锚点失败共 4 次（generator 空行×2、engine 空行×1、括号拼接事故×1），全部被"断言先于写盘"机制拦截或事后修复，最终文件均经 py_compile + pytest 验证。

## 二、未完成（M1 剩余）
1. **R23/R27/R29**（评价层：标签重编号/拒识/阈值分母）——位于 `tests/test_generic_pipeline.py` 205-215/318-335/460-489/746-787/1054-1073，随 `src/evaluation.py` 服务化一并修
2. **R30**（截断前缀时长用观测前缀）——advanced_extractor.py 60-132 区间
3. **S2 正式服务骨架**：`src/data_manifest.py`、`src/features/runtime.py`、`src/evaluation.py`、`src/reporting.py` 未建；test_generic_pipeline 薄化未做
4. **S5 独立推理**：`model_io.save_rule_bundle/load_rule_bundle`、`dpi_infer --rules`（不 import 训练库）
5. **S6 M1 端到端验收**：合成 fixture 三阶段 CLI、bundle/predictions/metrics/e2e/parity 五产物、独立进程无训练库运行
6. XGBoost 从正式 DPI 链路退位为对照（方案 0.3 第 4 条）——当前 engine.match 仍以 XGB 优先

## 三、下一步建议（按方案第 9 节顺序）
S2 服务骨架（含 R23/R27/R29/R30）→ S5 独立推理 → S6 验收清单逐条闭环 → M1 收口后进入 M2。

---
## 四、M1 端到端验收完成（2026-09-17 续）

### 三阶段 CLI 全链贯通
1. **mine**：`tests/test_generic_pipeline.py --train/--val/--test`（fixture：2 类 × 3 独立采集组 × 24 pcap）
   - 训练侧指标：召回/精确/准确 **1.0**、假阳率 **0.0**（R29 口径：阈值搜索含拒识惩罚）
2. **独立 detect**：`python -m src.engine.dpi_infer --rules bundle/ --pcap-dir test/`（subprocess 新进程）
   - 6/6 会话识别正确（appA×3/appB×3），置信度全 1.0，匹配耗时 0.01ms/会话
3. **evaluate**：metrics.json 从 predictions.json 计算（准确率 1.0）

### 产物（output/m1_e2e/）
bundle/{rules.json,selected_features.json,bundle_config.json,predictions.json}、metrics.json、parity.json、m1_e2e.log、final.log

### 关键验收点
- **parity：训练侧与推理侧同 pcap 319 维特征逐键一致（mismatches=0）**
- bundle 加载不 import xgboost/sklearn（load_rule_bundle 纯 json）
- 规则严格 AND 匹配（R18 语义生效）

### 过程中修复的三处新问题
1. `train_model` 的 `min_child_weight=3` 在小样本(12会话)下禁止一切分裂→概率恒0.5（诊断：13列全量0.5 vs 单列duration=1.0 → 参数消融定位）→ 改为1
2. `dpi_infer --pcap-dir` 的 `glob('*.pcap')` 不递归子目录 → `rglob`（类别子目录结构必需）
3. S5 补丁中 dpi_infer 参数段曾静默未落盘（冒烟未覆盖 CLI 层），S6 首跑暴露后以 span 法重放修复

### 未完全达成项（如实记录，留 M1+ 收尾）
- 独立环境/删模型文件运行的显式验证（理论无依赖，未做实机验证）
- src/data_manifest.py、src/features/runtime.py、src/evaluation.py、src/reporting.py 四模块未建（当前由 test_generic_pipeline 内函数承担语义，"一份实现"的正式服务化未完成）
- 树导出规则与原树一致性（W4 完整用例：概率并列等）未做专项测试
- R30 仍悬置（观测层语义，随 runtime 实现定义）

## 五、修复总计（截至本文档更新）
31 项中 **30 项已修并验证**（标尺 `tests/regression/test_review_regressions.py` 31 用例全绿：2 控制 + 29 反例，其中 R24 为弱断言、R30 未建用例）。R30 留观测层。
