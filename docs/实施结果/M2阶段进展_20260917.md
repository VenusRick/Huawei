# P1（M2）阶段进展：精细化行为观测（2026-09-17）

## 一、交付内容

### 1. src/parser/context.py（新增）
- BehaviorContextTracker：逐包事件聚合 -> 按 (capture_id, 可见终端) 维护
  5s 窗/1s 步长有限时间窗；窗内每条连接按活动间隔(2s)切流段；方向相对选定终端；
  **固定槽位 max_segments=3**（起始时间+首捕获包序号稳定排序）；
  overflow.aggregate_remaining=true（剩余流段进窗级统计不静默丢）；
  窗口发射即产出、不用未来包；R30 截断前缀写入观测
- WindowFeatureBuilder：41 维行为特征（window_* 窗级 11 / seg0-2_* 段级 21 /
  rel01/rel12_* 段间关系 10），缺槽 None 有效性标记（不零填）

### 2. 全链接线
- SessionManager 加 packet_listener（M2 逐包事件转发，最小侵入 2 处）
- runtime.extract_behavior_feature_records 共享提取入口（训练/推理同函数）
- mine behavior 分支：manifest 区间 -> label_window 窗口标签（重叠率>=0.5），
  background 窗不入训练（首版）；bundle 显式携带 context 配置
- dpi_infer：--terminal 参数 + bundle task=behavior 时窗口模式
  （bundle context 同参重算，None->-1.0 与训练侧哨兵一致）

### 3. 合成验收（tests/test_behavior_context.py 10 用例全绿）
跨流先后结构区分 / 两终端视角互补不合并 / 无未来包（逐包回调核验）/
长连接跨窗 / 分批一致 / 同刻稳定排序 / overflow 聚合不丢 / 缺槽 None /
特征名无标签泄漏 / label_window 区间映射

### 4. M2 端到端（output/p1_e2e/）
- fixture_m2：2 行为（voice_call=对称稳流 / text_chat=稀疏簇状）x 4 采集组
  x 20s（行为区间 2-18s，前后真背景心跳），split 按组不泄漏
- mine 退出0（5 规则 bundle）-> 独立 dpi_infer --rules --terminal（37 窗）->
  窗口级评价（window_metrics.json）
- **行为窗 32/32 全对（1.000）**；5 个错误窗全部是背景边缘窗（16-18s 行为
  尾部与背景混合）被误报——训练未含 background 负类所致

## 二、测试终态
71/71 全绿（标尺 38 + 契约 17 + 行为 10 + test_all 6）

## 三、已知边界与改进项（如实记录）
1. 背景窗误报：训练加 background 负类（或评价声明边缘窗不计分）——下轮迭代
2. context 配置来自 bundle：M2 验收"训练/检测同一窗口生成器"已由共享入口保证
3. 事件级评价（match_events 一对一容差）已实现未接入 CLI（真实动作记录到位后启用）
4. 真实行为标签子任务：待 P1.5 数据（合成链路已全通）

## 四、过程中的坑
- scapy wrpcap 不按构造序自动分时间戳：所有包几乎同刻（span 0.0148s），
  必须显式 pkt.time = base + offset
- M2 冒烟 terminal 误传 device_id（非 IP）导致方向全 down——manifest 增加
  terminal_ip 字段
