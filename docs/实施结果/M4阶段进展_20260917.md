# P3（M4）阶段进展：真实场景、协议回归与交付件（2026-09-17）

## 一、交付内容

### 1. G09 真实数据全链（CSTNET-TLS1.3 六类，运行中）
- 选材：overleaf.com(6)/huawei.com(4)/acm.org(3)/gitlab.com(3)/twimg.com(3)/
  vivo.com.cn(3) 共 20 pcap（1.7GB），split 按文件独立采集（train2/val1/test1）
- label_source 新增 dataset_official 档（数据集官方分文件标注，如实反映）
- validate-data 通过；mine->detect->evaluate 后台串联
  （output/p3_g09/，完成后以 evaluation/metrics.json 为准，不预填数值）

### 2. §5.1 TCP+TLS 联合用例（标尺 +3 用例）
- 完整/三段分段变体：ch_sni 提取一致、record 不双计
- 缺口不拼造：缺 record 头永不解析
- 四元组独立：新四元组不复用旧流残留
- 分层边界注明：乱序重排与重复段去重属 TCP 层（R03 用例已覆盖），
  _tls_feed 只收已排序去重字节流

### 3. G10 交付件五件套（docs/deliverables/）
工具技术说明书（架构+命令级使用+人工修正流程）/ 引擎技术说明书
（规则包格式、严格 AND、拒识语义、隔离性证据）/ 测试报告与部署手册
（74 用例+一键验收+e2e 实测+已知边界）/ 数据集清单与授权
（自有合成可交付；第三方仅清单；缺口如实）；CHANGELOG.md；
requirements-lock.txt（221 包）

## 二、测试终态
标尺 41 + 契约 17 + 行为 10 + test_all 6 = 74 用例全绿

## 三、M4 剩余（如实交接）
- 按需计算（算子组依赖链+调用计数）：未实现——引擎当前全特征计算后
  匹配，G08 只到"规则数/条件数/实际使用特征数"可报告层面
- 流式与资源上限（块读包/过期回收/上限配置）：未实现
- 性能表（吞吐/RSS/P95）：待 G09 完成后补充实测（负载高时测会失真）
- §5.3 QUIC 三态：未做（解保护超限可降级可见范围，按方案允许）
- report.html 为最小静态页

## 四、过程中的坑
- CSTNET 常见类（baidu/apple等）只有单文件无副本 -> 自动改选>=3副本类
- 标尺 SNI 键名为 ch_sni（非 sni）；P() 为位置参数签名
- 重复段/乱序在 _tls_feed 层预期越界（字节缓冲无 SEQ 感知）-> 分层修正


## 五、G09 真实成绩（2026-09-17 深夜补录）
- 事故与修复：初版 mine 对大会话无 500 包截断（runtime/dpi_infer 有），高会话数文件特征提取爆炸（9.5h 未完成）；且构成训练/推理特征不一致（parity 红线违规）。修复=mine Step2 与 runtime 同参截断。修复后 mine 从不可完成 -> 33 分钟（18 文件/783MB 前缀子集）。
- 小子集成绩（CSTNET 六类、每文件前 5 万包、6 test pcap/304 会话）：accuracy=0.8651 macro-F1=0.8609 macro-FPR=0.0151 拒识19 未配对0（完整分母，拒识计漏检；产物 output/p3_g09s/）
- 全量版（1.7GB/20 文件）修复后重启运行中（output/p3_g09/）
- 过程坑：pkill -f 自匹配杀死自身 shell（老坑重踩）；tcpdump 未装改 editcap 分卷法；CSTNET 每类文件均 80-95MB 挑小文件无效

## 五、G09 真实成绩（22:23-22:57 补记）
- 诊断链：大跑 10h20m 未完 -> 分诊实验（解析 63MB/s 无辜；特征 0.27s/会话）
  -> 真因：mine Step2 无 500 包截断（与 runtime/dpi_infer 不一致，parity bug）
  -> 修复后小跑 v3 全链 34 分钟（旧版同量级 67 分钟未过解析）
- 真实成绩（CSTNET 六类小子集：每类最小3文件、前5万包前缀，304 会话）：
  accuracy 0.865 / macro-F1 0.861 / macro-FPR 0.015 / 拒识 19 / 未配对 0
  分类别 F1：huawei 0.984 / twimg 0.954 / acm 0.911 / overleaf 0.857 /
  vivo 0.793 / gitlab 0.667(n=4)；17 条规则；产物 output/p3_g09s/
- 大跑全量版（修复后）22:58 重启，产物 output/p3_g09/

## 六、§5.3 QUIC 三态 + 回归终态（深夜补记）
- 三态用例 5 条：有效长头 Initial（版本/CID 可见字段）/ 短头不崩 /
  未知版本不虚构 / 普通 UDP 反例（DNS/空/短包）/ 三态独立不互伪装
- 修复真缺陷：短头解析缺 fixed bit 检查（RFC 9000 必须为 1），
  DNS 等普通 UDP 曾被误计为 1-RTT
- 协议属性如实记录：QUIC 短头（fixed=1）与随机 UDP 固有不可完全区分
- 解保护（RFC 9001 Initial）按方案允许维持"可见范围"口径，不虚标握手分析
- 回归终态：**79 用例全绿**（标尺 46 + 契约 17 + 行为 10 + test_all 6）；
  P4 三组（§5.1 TCP+TLS / §5.2 GREASE / §5.3 QUIC）全部落定


## 六、G09 全量版成绩（2026-09-18 凌晨）
- 修复版全量（1.7GB/20 pcap/6 test/617 会话）：mine 2h09m；accuracy=0.7050 macro-F1=0.7422 macro-FPR=0.0092 拒识154/617（25%计入漏检分母）未配对0
- 对比小子集（acc 0.865/F1 0.861/FPR 1.5%/拒识6%）：全量含长尾会话，边缘会话不命中规则致拒识升高，误报极低（严格AND+高阈值的保守性格）
- 改进路径（下轮）：阈值调优、规则覆盖扩展、背景负类训练；分母未动、成绩如实

## 七、G09 全量版成绩（01:09 落盘）
- 全链 2h11m（22:58-01:09）：mine 2h09m + detect 2min + eval 1s
- 全量（20 文件完整，617 会话）：accuracy 0.705 / macro-F1 0.742 /
  macro-FPR 0.009 / 拒识 154(25%) / 未配对 0
- 与小子集对比：全量 FPR 更低（0.009 vs 0.015），拒识率 25% 为主要拖累
  （长尾会话在严格 AND 规则下大量拒识=宁可拒识不误报语义）；
  两口径并存如实呈现，不粉饰
- 产物：output/p3_g09/（bundle/rules.yaml + analysis/ + evaluation/metrics.json）
- G09 至此完整闭环：小子集 + 全量双口径真实成绩 + 性能修复实证
  （10h 病灶版 -> 修复版 2h 全量/34min 小集）


## 七、全量拒识构成分析（标签粒度核对，2026-09-18）
- 拒识154中 gitlab.com_p003 单文件占111（该文件120会话拒111）；其余文件拒识个位数
- 命中置信度高（p50=0.991），阈值无辜；根因=覆盖不足
- SNI核对：gitlab_p003 全部307会话无TLS握手可见（中途抓包/纯加密数据段），与训练文件形态跨文件漂移，规则未覆盖——语义上属合理拒识
- 标签粒度问题：pcap级标签把第三方/CDN背景会话也计为本类，拒识计入漏检拉低召回（分母未动）
- 改进记录（不动当前成绩口径）：行为规则覆盖扩展/背景负类训练留待下轮；本分析作为G09成绩的解释证据


## 八、按需计算落地（G08 核心项，2026-09-18）
- src/features/operators.py：算子组定义（basic_counter/advanced_behavioral）、
  required_features_of_rules（bundle->规则引用特征）、resolve_required_operators、
  OPERATOR_CALLS 调用计数、extract_on_demand 分组调度
- dpi_infer --on-demand：按 bundle 解析激活算子组；未激活组零调用（计数留证）；
  不先算 319 维再取 3 维
- 验收（tests/test_on_demand.py 5 用例）：组映射/纯basic解析/混合解析/
  计数器只计激活组/全算vs按需输出逐项一致（label+confidence）
- 局限（如实）：首版两组粒度（方案允许组内一起算）；更细算子组拆分留待下轮


## 九、§5.3 QUIC 三态落地（2026-09-18）
- 5 用例入标尺：有效 Initial v1（version/DCID 正确）/ 未知版本 0xDEADBEEF 如实记录可见范围不虚标握手 / 短头 1-RTT 走统计降级不冒充 Initial / 普通 UDP（fixed bit=0）明确拒绝 / 截断包安全返回 None
- RFC9001 去头保护未实现（按方案允许报可见范围：版本+连接ID）


## 十、流式回收与 tgp 薄化收官（2026-09-18）
- SessionManager：max_closed_sessions 上限（超限丢最老+closed_overflow_dropped 计数）、evicted_sessions 证据计数+_evicted 标记（不伪装完整）、take_closed_sessions() 消费即释放
- PCAPReader：read_pcap_generator 全部关闭路径（返回值/FIN/超时/驱逐）统一交付且不重复不囤积（修复：completed 双交付缺陷）；TLS 缓冲全局上限 64MB+tls_buffer_evicted 计数
- 验收：tests/test_streaming_recycle.py 5 用例（evict 证据/overflow 计数/take 清空/流式不囤积/TLS 上限）
- tgp 薄化第一步：runtime 新增 extract_features_from_sessions 共享 helper（单一实现），tgp 的 extract_features 变薄包装——防双实现漂移；消费方验证 23/23 全过（含 parity 前哨）
- 全套回归 94/94 全绿
