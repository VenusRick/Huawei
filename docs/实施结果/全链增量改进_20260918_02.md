# 全链增量改进报告（2026-09-18 第二轮 · chain_improve_20260918_02）

> 基线：HEAD `363a2a0` + 用户未提交修改（未 reset/revert/覆盖），宿主基线快照
> `output/chain_improve_20260918_02/baseline/`（preexisting.patch / source_hashes.json）。
> 上一轮报告：`docs/实施结果/全链小批量调改_20260918.md`（122 用例全绿）。
> 本轮性质：**核实宿主下一层问题 → 实际修复 + 反例回归 + 同输入前后对照**。
> 纪律执行：TRAIN_THREADS=2、OMP/BLAS=2、单命令≤150s、树参数候选仅4个
> （train拟合/validation选择/test一次）、未跑1.7GB全量、无后台遗留任务。
> 机器可读核账：`output/chain_improve_20260918_02/reports/competition_accounting.json`。

---

## 一、宿主下一层问题逐条核实结论（先反例、后修复）

全部 14 项主张先以反例脚本核实
（`output/chain_improve_20260918_02/probes/verify_bugs.py`，修复前运行），
**13 项属实并修复，1 项核实为无泄漏并补回归钉死**：

### 1) 评价层（evaluation.py）

| # | 主张 | 核实 | 修复 |
|---|---|---|---|
| E1 | 单行JSONL预测对象整体json成功却返回空 | **属实**：`{"predicted_label":...}` 单行被当"无results"报"文件为空"（误导性报错）；带统计键的dict静默空 | 无`results`包装但含预测键(`predicted_label`等)→按1条预测；两者皆无→显式报错列出期望键 |
| E2 | 词表外数字ID被confusion_matrix静默丢分母 | **属实**：`y_pred=[0,7,1,1,9]`时`cm.sum()=3≠5`，2样本消失；rejection_rate=0（词表外≠拒识却被吞）；macro-FPR分母缩水 | 词表外ID映射哨兵`OOV=-2`独立列/行入矩阵（分母=n不缩水）；`metrics["out_of_vocab"]`单列pred/true计数；拒识(unknown)与词表外不混同 |
| E3 | align只遍历预测，漏输出truth文件不暴露 | **属实**：真值b.pcap无任何预测→accuracy=1.0假象完全不可见 | `truth_coverage={n_truth_files,n_covered_files,missing_files[{file,label,trial_id}]}`入metrics；漏文件不产假分母 |
| E4 | 重复observation入分母 | **属实**：同一`a.pcap:0`两行计n_samples=2 | 真实observation_id重复只计首条+`n_duplicate_observations`计数；无observation_id旧行按行保留（不把同文件多会话误判重复） |
| E5 | 行为aggregate把background当事件、只按文件不按terminal分组 | **属实**：两终端同窗同标签并成1事件；background预测成行为事件进FP | 按`(source_file, terminal)`分组；`background`预测不入事件序列（只进背景FPR口径） |
| E6 | match_events不隔离source跨试次误配；贪心非最大一对一少算TP | **属实**：trialB预测配到trialA真值(tp=1应为0)；贪心反例`preds t=[0.9,1.05] truths t=[0,1.0] tol=1`贪心tp=1、最大=2 | 双侧带source_file时按文件名隔离（旧API无source保持原语义）；最大一对一配对（主机复验后改为非递归增广路算法，始终最大一对一配对，不依赖scipy.optimize） |
| — | 全拒识/空预测/漏文件/重复observation合理处理 | — | 全拒识：拒识率1.0+各类recall=0（完整分母，新增回归）；空预测/空truth：显式报错不产假指标（保持）；漏文件=E3；重复=E4 |

### 2) 读取/截断因果（pcap_reader.py / session_manager.py / runtime.py）

| # | 主张 | 核实 | 修复 |
|---|---|---|---|
| R1 | 正式入口仍整批read_pcap（generator闲置） | **属实**：`extract_feature_records_with_stats`调`reader.read_pcap`全列表 | `read_pcap_generator(file, max_read_packets)`成为唯一读取路径；`read_pcap=list(generator)`（顺序/逐包异常计数/read_capped统计**按构造一致**）；正式提取入口逐会话消费（closed即取即弃），会话对象不囤积 |
| R2 | PCAPNG整文件读 | **属实**：`head+f.read()`整载 | 按块增量读（`_iter_pcapng_blocks`+`_decode_pcapng_block`流式/整读共用单一解码器；section字节序/接口表状态跨块保持；**真读包上限对PCAPNG生效**；残缺尾块按截断停止不崩；与整读版逐包相等——回归钉死） |
| R3 | 截断视图保留全会话重传/乱序计数（未来事件污染） | **属实**：重传发生在截断点之后，`view.num_retransmissions=1`（因果值0） | `_append_packet`记录每包ingest时刻累计快照（`cum_retransmissions/cum_out_of_order`）；截断视图取前缀末包快照——截断点之后观测的事件不再泄入；快照缺失（手工构造会话）回退保留原值不伪造归零。资源上限证据：`sessions_evicted/sessions_overflow_dropped`入`extraction_stats.json`（正常0，非0可见） |
| R4 | TLS重组是否回填前缀包泄露未来握手 | **核实无泄漏**：`_tls_feed`只给"凑满record的包"设tls_info（前2段None、末段set），前缀截断后前缀内无握手即如实无握手 | 新增回归`test_tls_reassembly_no_prefix_backfill`钉死该因果保证 |

### 3) 规则语义（optimized_generator.py / optimized_engine.py / pipeline.py）

| # | 主张 | 核实 | 修复 |
|---|---|---|---|
| U1 | 统计规则confidence=coverage(召回)非precision；零覆盖死规则照部署 | **属实**：审计bundle含`STAT_0001/0002 conf=0.0`（训练0命中）部署在册；STAT(qq)实际train precision=1.000却标0.710 | confidence=train precision；`train_coverage/train_precision`入规则元数据；**零覆盖规则不部署**；条件特征只用训练完全present特征（训练fillna(0)造的阈值与推理"缺失即不匹配"（R22）语义冲突——不再用0填对齐，缺特征的特征不进规则条件） |
| U2 | 浅树固定depth5/leaf5小样本欠拟合 | **属实**：41样本6类只导出1条树规则、覆盖1/6类 | `leaf=max(1,min(5,n//(2k)))`自适应（大样本行为不变、小样本放宽）；depth默认仍5（见下节实验：加深无收益不采纳） |
| U3 | priority无人消费、conf=0规则照发预测 | **属实**：`match()`按列表顺序遍历、不看priority；conf=0.0命中返回`reported_conf=0.0`算"已识别"（阈值0.95形同虚设） | 规则按priority**升序**评估（DT100先于STAT200，确定性顺序不依赖列表）；规则命中须`confidence>=confidence_threshold`，低于阈值按未匹配处理且不阻断后续规则 |
| U4 | （自查发现）mine侧验证集回放用引擎默认阈值0.70，独立推理用bundle 0.95 | **属实**（口径不一致） | `_replay_validation_raw`阈值=生成器/bundle阈值（与dpi_infer同口径） |
| D1 | 按需算子成本只在假extracter上测过 | **属实**（CLI未输出计数） | `--on-demand`真实CLI输出`operator_calls`（见性能节实测） |

### 受控实验（候选≤4，train拟合/validation选择/test一次）

浅树参数候选（同输入`raw_features.csv` train41/val88，统一新语义+阈值0.95）：

| 候选 | 参数 | 树规则(类) | train acc/F1 | **validation** acc/F1/rej/FPR |
|---|---|---|---|---|
| C0 | depth5 leaf5（旧） | 1(1类) | .707/.161 | .000/.000/.239/.127 |
| C1 | depth5 leaf自适应 | 2(1类) | .707/.161 | .000/.000/.239/.127 |
| C2 | depth6 leaf自适应 | 2(1类) | .707/.161 | .000/.000/.239/.127 |
| C3 | depth8 leaf自适应 | 2(1类) | .707/.161 | .000/.000/.239/.127 |

**结论（如实）**：四候选验证集回放完全相同——该数据本轮观测到训练类不平衡与规则覆盖不足
（qq.com占31/41训练行；其余五类分别为4/1/3/1/1行，规则只会认qq），不是树深/叶子。**采纳C1**（自适应叶子
是正确性修复：6类×4样本时固定leaf=5根本无法为小类产纯叶，回归可证；大样本
行为不变）；**不采纳加深**（C2/C3无差异）。记录：`probes/tree_candidates.json`。
验证集0准确=规则在验证集上"硬预测全错"（67次命中0正确），如实写入
`validation_report.json`，不删难类不调阈值掩盖。

---

## 二、同输入前后对照（cstnetqq 前2000包×17文件，与审计轮完全同输入同划分）

| 项 | 审计轮（修复前） | 本轮（修复后） | 说明 |
|---|---|---|---|
| bundle规则数 | 5（含2条conf=0死规则） | **4**（死规则剔除+自适应多1条树规则） | STAT置信度=precision(1.0)+coverage/precision元数据 |
| 规则条件特征用量 | 30选中 | 30选中/**8入条件** | 引用计数可查`used_by_rules` |
| 提取会话分母 | 184/过滤55/截断12/失败0 | **184/55/12/0（逐项一致）** | 流式改造不丢不增会话 |
| parity | 319维0失配 | **319维0失配** | 提取路径改生成器后复验 |
| test（10会话完整分母） | acc .100 / macroF1 .111 / FPR 1.85% / 拒识8 | **acc .100 / macroF1 .111 / FPR 1.85% / 拒识8** | 死规则本就未在此测试集触发——本轮修的是潜在误报源与语义，不是该集分数 |
| 评价输出 | n_unpaired | +`truth_coverage`(6/6,missing=[])、`n_duplicate_observations`(0)、`out_of_vocab`机制 | 漏文件/重复/词表外不再隐形 |
| 预测可追溯性 | 仅source_file | **每行带observation_id**（file:session） | dpi_infer app模式 |

证据：`output/chain_improve_20260918_02/{mine,predictions.json,evaluation}/`；
上一轮对照 `output/chain_audit_20260918/fixes/`。

**行为链路（fixture_m2，g4测试split，完整分母37窗）**：
acc .865 / macroF1 .907 / macroFPR 0；背景FPR **0/5**；事件级
**tp=2 fp=0 fn=0（recall=precision=1.0，source隔离+最大一对一）**。
上一轮g3验证split背景类实验acc .946——测试split略低为正常泛化差，如实呈现。
证据：`output/chain_improve_20260918_02/behavior/`。

---

## 三、性能实测（小批量口径，与审计轮同法可直接对比）

`output/chain_improve_20260918_02/perf/perf_small_batch_after.json`
（17文件×2000包上限、139记录、Python3.10、TRAIN_THREADS=2）：

| 项 | 审计轮 | 本轮 | 变化 |
|---|---|---|---|
| 解析+特征提取 wall | 18.72s | 19.57s | 持平（噪声带内；mine端到端两次17.26/15.84→22.55/15.84亦证明运行间噪声±数秒） |
| **提取峰值RSS** | 47.1MB | **41.0MB（−13%）** | 流式消费closed即取即弃的直接收益 |
| 规则匹配 P50/P95/P99 | 0.0062/0.0133/0.0247ms | **0.0062/0.0132/0.0235ms** | 持平；吞吐≈13.6万次/s |
| mine 端到端 wall/RSS | 17.26s/283MB | 15.84s/284MB | 持平 |

**独立推理纯净性（子进程实测）**：`probes/independence_probe.py` 在子进程
meta_path 封锁 `sklearn/xgboost/scipy` 导入后，加载 app bundle
（4规则/30特征/阈值0.95）→ 提取319维 → 规则匹配：10预测2命中，**0次违规**；
behavior bundle 同法 0 违规。退出码即证据。

**按需计算（真实独立CLI，非假extracter）**：`--on-demand` 实测只需
`basic+adv_multiscale+adv_direction+adv_complexity`（跳过
distribution/cross两族，调用计数入输出JSON：`{basic:31, multiscale:31,
direction:31, complexity:31, advanced:0}`）；与全算预测**逐条相等**；
wall 2.02s vs 全算1.97s——**如实声明：该bundle所需族含最贵的complexity，
按需省调用面不省时**，不夸大为性能优化。

---

## 四、赛题逐项核账（阈值来自题面；机器可读版见 reports/competition_accounting.json）

| 题面要求 | 状态 | 本轮证据与口径 |
|---|---|---|
| 自动特征输出有效率≥80% | **FAIL（实测）** | 严格口径 E=18/A=319=5.6%；宽松口径（不计bundle可达集）144/319=45.1%。**与上轮严格可比（A=319、E=18未动）**，未删特征未改口径凑数。根因：小样本判别力+bundle只部署30特征 |
| 人工修正后可用率≥90% | **BLOCKED_DATA** | 需真实人工修正记录；revise-rules通道本轮再次验证（禁用STAT_0001→回放88样本21条预测变化→新版本不覆盖原bundle），无人工记录即不可验证 |
| Recall≥98/Acc≥95/P≥95/FPR≤5% | **BLOCKED_DATA** | 官方场景=社交文字贴/IM文字·语音·视频通话/Tor/Psiphon/Session且需三平台；现有数据是CSTNET六类**网站流量**，只作工程验证（test acc .1等见上表），**不得报为场景达标**。行为合成fixture（g4测试split）acc .865/背景FPR 0/事件全对——同样只是链路验证。指标分母口径：完整分母含拒识、漏检计错、背景FPR单列、事件按同源+同终端最大一对一 |
| Android/iOS/PC | **BLOCKED_DATA** | 清单全部platform=pc；iOS/Android未采集 |
| 工具+独立规则DPI | **PASS（工程）** | 子进程封锁训练库导入实测0违规；吞吐13.6万匹配/s；bundle三件套加载即推理 |
| 两说明书/测试部署/标注数据 | **部分** | 前次交付初稿在库；本轮规则置信度语义/阈值口径/评价输出字段有变，主机已增量同步两说明书、测试部署手册和CLAUDE；历史测量明确留档，真实标注数据仍缺 |

**不报"全通过/整体满足赛题"**：工程修复与业务达标分开陈述；
未测项（全量1.7GB重跑）如实标 not_measured。

---

## 五、修改文件清单（本轮）

代码（9，含主机复验追加的 operators.py）：
- `src/evaluation.py` — E1-E6（单行预测对象/OOV接管/truth_coverage/重复观测去重/事件终端分组+背景排除/source隔离+最大一对一）
- `src/parser/pcap_reader.py` — R1/R2（统一有界生成器、PCAPNG按块流式解码、read_pcap薄包装）
- `src/parser/session/session_manager.py` — R3（包级ingest累计快照）
- `src/features/runtime.py` — R1/R3（生成器消费、因果截断计数、资源上限计数入stats、行为路径同样有界）
- `src/engine/rule_compiler/optimized_generator.py` — U1/U2（precision置信度+零覆盖剔除+present特征约束+自适应浅树参数）
- `src/engine/matcher/optimized_engine.py` — U3（priority升序评估+规则路径应用阈值）
- `src/engine/dpi_infer.py` — D1（observation_id入输出、on-demand算子计数入输出JSON、结果写出统一helper）
- `src/pipeline.py` — U4（验证集回放阈值与bundle一致）

测试（1新增，18用例）：
- `tests/regression/test_chain_improve_20260918_02.py` — 上述每项修复的反例守门
  （单行JSONL/OOV分母/覆盖/去重/事件两案/生成器入口/PCAPNG上限与流式一致/因果重传/TLS不回填/precision语义/present特征/阈值/priority/自适应树/全拒识）

文档（2）：本报告、`docs/进度.md`（追加第二十七节）。

**全套 140 passed**（既有122全保留+新增18）；`tests/test_all.py` 脚本入口 6/6。

未改动：`data/` 原始数据与清单（外部只读软链接原样）、旧 output 全部目录、
用户已有修复均保留；交付说明在原内容上增量同步，没有reset/revert。

## 六、失败候选与未覆盖边界（如实）

1. 树深候选C2/C3（depth6/8）：验证集与C0/C1完全相同→**不采纳**（瓶颈是
   小样本类平衡不是深度）；
2. 按需计算省时：该bundle规则引用complexity族特征，**无省时收益**（只省
   调用面2族），保留功能不宣传性能；
3. 阈值0.95来自配置未调——不借调阈值/删难类把验证集0准确"修"好看；
4. 未覆盖：全量1.7GB重跑（预算排除）；真实三平台/官方场景数据（BLOCKED_DATA）；
   交付文档正文已增量同步；旧测量保留为历史记录；
5. 评价端`out_of_vocab`为防御机制——当前正式链路预测全部来自bundle词表，
   未在真实数据触发（回归测试以构造数据钉死语义）。

## 七、证据路径索引

- 反例核实（修复前运行）：`output/chain_improve_20260918_02/probes/verify_bugs.py`
- 树参数候选实验：`probes/tree_candidates.json`
- 主链产物：`mine/`（含bundle/validation_report/parity_report/extraction_stats/analysis）、`predictions.json`、`evaluation/`
- 行为链产物：`behavior/`（manifests/mine_bg/predictions_test/evaluation）
- 性能：`perf/perf_small_batch_after.json`、`perf/ondemand_qq.json`、`perf/fullcalc_qq.json`、`mine.log`/`mine_run2.log`
- 独立性：`probes/independence_probe.py`（退出码0=纯净）
- revise-rules演练：`revised/`（revision_diff.json）
- 测试隔离：`tests/pytest_tmp*`、`test_all_out/`

## 八、主机独立验收与追加修复（最终结果）

主机不是仅采纳Agent汇报：重新运行全部测试，重走独立CLI，重算预测指标，
并逐项核对实际训练计数。最终为 **145 passed、2项既有scipy常量输入警告**。
本轮共新增23项回归（Agent18项+主机5项），原122项全部保留。

### 8.1 实际部署依赖修复
发现按需调度仍将 `selected_features` 全目录并入条件依赖：30个候选中只有8个
被可执行规则引用，却仍调用未引用的Hurst等昂贵特征。已去掉该并集，跳过ENS
占位规则。候选/人工编辑目录保持30维；实际只需basic和adv_direction。

同一真实QQ PCAP（前2000包、31条合格会话），3次交错新进程实测：

|模式|端到端CLI耗时中位数|峰值RSS中位数|
|---|---:|---:|
|全特征计算|2.4342s|36.00MiB|
|仅规则依赖|0.2439s|35.00MiB|

该小文件实测加速 **9.98×**，不是全数据/网络线速结论。
三个重复全部预测、置信度和观测标识相同。算子计数basic=31、adv_direction=31，
其他高级族为零/未调用。第三节“无省时收益”是本次追加修复前的中间观测，
已由上述同机同输入重复测量替代；它暴露的原因正是依赖目录混淆。

### 8.2 评价与独立性复验
- 补齐仅有observation_id时文件覆盖误判；新增evaluation_scope，缺文件等情形标incomplete。文件覆盖不等于已验证全部会话/窗口分母。
- 去掉事件匹配的scipy→贪心降级；用非递归增广路始终最大配对，30个小随机二分图与穷举结果一致。
- 阻断sklearn/xgboost/scipy导入后，正式CLI分别重放6个应用测试PCAP及2个行为测试PCAP。禁止库导入尝试=0、已加载=0；本次行为测试确实走behavior窗口入口。
- 重新预测的10条应用结果、37条行为窗口与本轮原结果逐条一致。复算Accuracy/P/R/F1/FPR全部不变，数据/划分/阈值未改。
- 应用train41/validation88/test10；qq.com为31/41训练行，三个类别各只有1行。部署可执行规则只有qq类，validation为67次命中、0次正确。该小批次规则泛化未改善，不能只用低macro-FPR宣称有效。

最终证据：host_verify/final_pytest.log、dependency_cli_benchmark.json、
independence_and_parity.json、app_evaluation/metrics.json、behavior_evaluation/metrics.json。
全链大规模吞吐、真实比赛动作及三平台仍未验证；自动特征有效率18/319为项目内部
严格代理口径，题面未给出其计算公式，不能当作官方已核验评分。
