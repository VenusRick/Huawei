
---
## 十一、会话更新（2026-09-16 夜 · 检视整改）
- **外部检视整改**：01_代码检视报告（A01-A16）+ 02_执行与验收方案 + review_results.json（33 用例 31 fail）
- **已修复 14 项**（R01-R15/R17）：解析层 R01-R09、协议层 R10-R14、特征层 R15/R17；逐项合成验证全 PASS；`tests/test_all.py` 6/6 零回归
- **剩余 17 项**根因已定位（R16/R22/R24/R30 特征schema与污染、R18-R21/R31 规则引擎、R27/R23/R29 标签与阈值、R25/R28 mine入口、R26 配置往返），交接详见 `docs/handover/handover_20260916_检视整改.md`
- sample_debug 全链路验证后台进行中（`output/sample_debug_run.log`）

---
## 十二、会话更新（2026-09-17 · M1 增量整改）
- 新方案《全流程贯通与赛题交付_增量实施方案》启动，M1 阶段
- 自建回归标尺 `tests/regression/test_review_regressions.py`（28 用例，方案包的 reference_regressions 未随包落库）
- 新修 10 项（R16/R18-R22/R25/R26/R28/R31 + A01 标签回溯），**累计 26/31**；标尺 28/28、test_all 6/6 双绿
- M1 剩余：评价层 R23/R27/R29、R30、正式服务骨架、独立推理 --rules、端到端验收——详见 docs/实施结果/M1阶段进展_20260917.md

---
## 十三、会话更新（2026-09-17 续 · M1 三阶段贯通）
- M1 端到端验收完成：mine→独立规则detect(--rules)→evaluate 全链，fixture 2类×3采集组，训练侧与独立推理均 100%
- parity 319 维特征零失配；bundle 三件套+predictions+metrics+parity 产物齐
- 过程修 3 处：train_model min_child_weight 小样本禁分裂、dpi_infer pcap-dir 不递归、S5 补丁静默失败重放
- 累计 30/31 修复（R30 留观测层）；标尺 31 用例全绿
- 详见 docs/实施结果/M1阶段进展_20260917.md 第四、五节

## 十四、P0：统一契约底座收官（2026-09-17）
- 四服务模块（data_manifest/runtime/evaluation/reporting）落库，17 契约测试全绿
- CLI 新增 validate-data/evaluate；mine 接 manifest+产出 bundle 三件套
- scripts/run_acceptance.py：G01-G05 全 PASS（acceptance_p0_004）
- GREASE 精确集（5.2）+ W4 树一致性 4 用例入标尺（38/38 绿）；test_all 6/6
- configs 两件 + fixture manifest（18+6）+ all_data 1762 候选标签盘点
- 详见 docs/实施结果/P0阶段进展_20260917.md

## 十五、P1（M2）：精细化行为观测收官（2026-09-17）
- context.py（窗口/流段/K=3 槽位/overflow）+ packet_listener 接线 + runtime 共享入口
- mine behavior 分支 + dpi_infer --terminal 窗口模式，bundle 携带 context 配置
- 合成验收 10 用例 + e2e：行为窗 32/32（1.000）；背景边缘窗误报为已知边界
- 全套回归 71/71；详见 docs/实施结果/M2阶段进展_20260917.md

## 十六、P2（M3）：AnalysisPackage + revise-rules 收官（2026-09-17）
- 统一八件套双层生成（mine层3+evaluate层5）；特征有效率 E/A 口径固定留档
- revise-rules：结构化编辑校验+验证集回放 diff+版本化发布不覆盖
- e2e 实证全过（非法编辑明确拒绝）；71/71 全绿
- 详见 docs/实施结果/M3阶段进展_20260917.md

## 十七、P3（M4）：场景矩阵+协议回归+交付件（2026-09-17）
- G09：CSTNET 六类 20pcap 真实全链后台运行（label_source 新档 dataset_official）
- §5.1 TCP+TLS 联合 3 用例入标尺（41+17+10+6=74 全绿）
- G10 交付件五件套 + CHANGELOG + requirements-lock(221包)
- M4 剩余：按需计算/流式上限/性能表/QUIC 三态（如实交接）
- 详见 docs/实施结果/M4阶段进展_20260917.md

## 十八、G09 真实成绩落地 + mine 截断修复（2026-09-17 深夜）
- 修复 mine 大会话无截断（训练/推理特征不一致=parity违规）：9.5h不可完成 -> 33min
- 小子集真实成绩：acc 0.865 / macro-F1 0.861 / FPR 1.5%（304会话，完整分母）
- 全量 1.7GB 修复版重启运行中；坑：pkill -f 自匹配、tcpdump 缺改 editcap

## 十九、G09 全量版收官（2026-09-18 凌晨）
- acc 0.705 / macro-F1 0.742 / FPR 0.9% / 拒识25%（617会话，完整分母）
- 与小子集对比：拒识升误报降；改进路径=阈值/覆盖/负类；G01-G10 全有实测证据

## 二十、拒识构成分析+G08性能表（2026-09-18）
- 拒识72%来自gitlab_p003无握手会话（跨文件形态漂移，合理拒识）；阈值无辜(p50=0.991)
- 性能表落 output/performance.json（解析63MB/s/匹配0.01ms/会话/mine RSS 2.4GB）

## 二十一、按需计算落地（2026-09-18）
- operators.py 算子组调度 + dpi_infer --on-demand + 5 验收用例（输出一致性过）
- 全套回归见本轮；M4 剩余：流式回收/QUIC 三态/tgp 薄化

## 二十二、§5.3 QUIC 三态收官（2026-09-18）
- 5 用例全过（有效/未知版本可见范围/短头降级/UDP反例/截断安全）
- M4 仅剩：流式+资源上限、tgp 薄化；P4 三组协议用例全部完成（5.1/5.2/5.3）

## 二十三、M4 收官：流式回收+tgp 薄化（2026-09-18）
- 资源上限三件套（closed 上限/evict 证据/TLS 缓冲 64MB）+ generator 全路径交付不囤积；5 用例
- tgp extract_features 薄包装 runtime 共享 helper；94/94 全绿
- 方案 M4 至此除真实采集数据外全部落地

## 二十四、TRAIN_THREADS 回归测试（2026-09-18）
- 新增 `tests/test_train_threads.py`（5 用例，子进程隔离 import 缓存）：默认值收敛 1..8、环境变量显式覆盖生效（含 >8 不截断）、0/负值兜底为 1
- 定向运行 5/5 绿；未改生产代码

## 二十五、成绩归因+A实验（2026-09-18）
- 归因闭环：gitlab病类（全无握手）拖累19点；剔除后五类0.874/0.897
- A实验（换qq）反转更差0.28：特征选择重洗+类互扰；CSTNET结构性风险实锤
- 交付口径建议：分组报告；可选扩容/类平衡实验待定

## 二十五、B口径入交付件（2026-09-18）
- 测试报告采用分组呈现：握手可见五类 0.874/0.897 + 无握手段如实标注；CHANGELOG 更新
- 成绩呈现定稿，工程侧全部收口

## 二十六、全链审计+小批量调改（2026-09-18，本轮）
- **七类缺陷核实并修复**（标签泄漏/前缀截断未来包/评价分母/报告假证据/DPI入口/CV泄漏/背景类）：详见 `docs/实施结果/全链小批量调改_20260918.md`
- mine 重写：清单记录→精确标签（不猜文件名/不回落类0）；**train-only 拟合**，validation 仅作对照与证据（`validation_report.json`/`parity_report.json`/`extraction_stats.json` 落档）
- runtime 单一实现：`extract_feature_records_with_stats` 共享入口（mine/detect/dpi_infer 三方薄包装）；前缀截断不再用未来包时长、不污染原会话；解析过滤/截断/失败计数留证；`--max-read-packets` 真读包上限（PCAP 增量读）
- 评价修复：JSONL/单JSON 显式区分、重名真值报错、非连续标签ID对齐、行为逐窗 label_window 真值+背景FPR+事件一对一匹配
- 报告去假证据：consistent/engine_supported 无证据即 pending；ANOVA 证据集标注（validation）；class_profiles 键修复；误判清单含漏检
- 规则生成：废除固定5折CV（按会话泄漏），改 validation 优先→GroupKFold（组类重合守卫）→明确跳过；XGBoost 成绩仅离线对照
- 行为背景类选项 `--include-background`：合成验证 acc .865→.946、macroFPR .119→.048、背景FPR 1.0→0.4（默认关闭保留旧口径）
- 算子细化到特征族（adv_multiscale/direction/distribution/complexity/cross），昂贵族仅在需要时调用，调用计数+parity 验证
- 小批量前后对比（同输入同划分 cstnetqq 前2000包）：test acc 0.0→0.1、macroF1 0.0→0.111、FPR 5.6%→1.9%；全量结论不变（小样本弱规则如实呈现）
- 测试：新增 `tests/regression/test_chain_audit_20260918.py`（20用例）；全套 122 passed；`test_all.py` 假阳性修复（return bool→assert，6/6 保持）
- 官方指标诚实核账：特征有效率（严格口径 18/319=5.6%、宽松口径 45.1%）**未达80%**；六类网站流量不冒充比赛场景（社交/IM/Tor无真实三平台数据 → BLOCKED_DATA）

## 二十七、下一层问题修复+同输入前后对照（2026-09-18 第二轮）
- **宿主下一层14项主张逐条反例核实：13项属实修复、1项（TLS回填）核实无泄漏并钉死**：详见 `docs/实施结果/全链增量改进_20260918_02.md`；产物 `output/chain_improve_20260918_02/`
- 评价：词表外ID按OOV(-2)独立列入混淆矩阵（分母不缩水+计数单列）；truth_coverage/missing_files 暴露漏检文件；重复 observation_id 去重计数；行为事件按(文件,终端)分组+background不算事件+source隔离+**最大一对一配对**（贪心会少算TP的反例已钉）
- 读取：`read_pcap_generator` 成为唯一读取路径（read_pcap=薄包装，顺序/统计按构造一致）；**PCAPNG 按块增量读**（真读包上限对pcapng生效）；正式提取入口逐会话消费——**提取峰值RSS 47.1→41.0MB（−13%）**，会话分母计数逐项不变（184/55/12/0）、parity 319维0失配
- 截断因果：前缀视图重传/乱序取**截断点包级ingest快照**（上一轮"保留全会话计数"的已知边界已修；未来重传不再泄入）；TLS 重组确认不回填前缀包（回归钉死）
- 规则语义：统计规则 **confidence=train precision**（原为召回）+零覆盖死规则不部署（审计bundle的2条conf=0死规则已消失）；条件特征只用训练完全present特征（消除训练0填vs推理缺失不匹配的语义冲突）；引擎**按priority升序评估**且**规则路径应用置信度阈值**（conf<阈值不再照发预测）；mine侧回放阈值与bundle一致（原0.70 vs 0.95口径不一致已修）
- 浅树自适应：leaf=max(1,min(5,n//2k))；候选实验C0-C3（train拟合/validation选择）验证集完全相同→瓶颈是小样本类平衡非深度，**不采纳加深**；`--on-demand` 真实CLI算子调用计数入输出JSON（该bundle无省时收益，如实记录）
- 同输入前后（cstnetqq 17文件）：bundle 5→4规则（死规则剔除）；test 指标不变（acc .1/macroF1 .111/FPR 1.85%/拒识8——死规则本就未触发，修的是语义与潜在误报源）；行为fixture g4测试split（首次触碰）：acc .865/背景FPR 0/5/**事件tp=2 fp=0（source隔离+最大配对）**
- 独立性实测：子进程封锁 sklearn/xgboost/scipy 导入后加载bundle+319维提取+匹配，app/behavior两bundle均0违规
- 测试：新增 `tests/regression/test_chain_improve_20260918_02.py`（18用例）；**全套 140 passed**；test_all 6/6；官方核账（`reports/competition_accounting.json`）：特征有效率严格口径 E=18/A=319=5.6% **未达80%**（与上轮严格可比）；人工可用率/场景指标/三平台仍 BLOCKED_DATA；CSTNET与fixture数字只作工程验证不报场景达标

### 第二十七节主机复验补充（2026-09-18）
- 追加修复按需依赖：30候选仅8个实际条件特征；不再计算未引用的complexity/multiscale。真实31会话文件3次新进程中位CLI耗时2.4342→0.2439s（9.98×），逐条预测与置信度不变。
- 评价补齐observation-only覆盖和incomplete标记；事件匹配改为无scipy依赖的最大增广路算法。
- 独立CLI重新预测并复算app10条/behavior37窗，指标不变；禁止训练库导入尝试0。
- 全套145 passed（新增23项），两说明书/测试部署手册/CLAUDE已增量同步。
- 真实网站小批次准确率仍10%，合成行为86.49%；未取得业务达标提升。人工修正记录和真实三平台行为数据仍缺。

## 二十八、快速特征与小批量DPI优化（2026-09-18 第三轮 fast_dpi_20260918_03）
- **执行计划 S0-S3 全部完成**（计划SHA256核验一致）：详见 `docs/实施结果/快速特征与小批量DPI优化_20260918_03.md`；产物 `output/fast_dpi_20260918_03/`（含 acceptance.json/reproduce.sh/status.json）
- **S0 类平衡**：QQ 31/41失衡→每类20/10/10固定观测（239条，seed=20260918）；采集组=网站（_p002+为100MB滚动分片核实）；流族同split隔离+族cap6；huawei类覆盖不足升级扫描（总量289,604包≤50万硬上限）；已见诊断集+同采集组会话级划分如实声明
- **S1 双端真少算**：fast16/fast32 profile（16/32低成本特征，32包前缀）；extractor按名提取（公式单一来源，未请求族0调用）；bundle携带profile/前缀，CLI默认读取；封锁训练库正式CLI exit=0；旧319维保留兼容默认
- **S2 候选**：C1(fast16×不加权,thr0.9)选中（tier3：maxFPR≤5%）；发现balanced加权在类平衡抽样下为数值空操作（C2≡C1）；C5(64包前缀)FPR恶化至16%拒绝；C6 Unused（无证据单特征弥合recall缺口）
- **S3 冻结测试（一次）**：59观测0缺行；**acc 28.8% / P 67.9% / R 28.7% / max逐类FPR 6.1%——四目标均未达**（结构性：capture级标签污染+小样本+类间重叠）；六类均有可触发规则（前轮仅qq）
- **性能（归因分列）**：同前缀同会话集特征裁剪 10.66→7.83ms/记录（1.36×）；部署口径端到端2.93s→0.31s（9.61×，前缀500→32为主）；不冒充
- **C/A/E/R新口径**：C=32/A=16（冻结）/E=5（31.2%）/R=13；旧18/319为历史审计口径不混同；人工90%与官方场景/三平台 BLOCKED_DATA
- 测试：新增 `tests/regression/test_fast_dpi_20260918_03.py`（8用例）；**全套153 passed**；代码改动：fast_profile.py(新)/feature_extractor(requested)/operators(fast组)/dpi_infer(bundle默认profile+--max-read-packets)/pipeline(--profile+--observations)/evaluation(观测级评价)/generator(tree_class_weight+真实precision回放)

## 二十九、扩类60：数据审计+通用索引+64包×64维验证（2026-09-18 第四轮）
- **根因修复**：s0_index.py SITE_FILES 硬编码六类导致扩类索引全错；新增 `scripts/cstnet_catalog.py` 按文件名模式自动发现（非贪婪正则剥离 `_pNNN`，六类与旧清单逐字兼容有测试钉死），`cstnet_survey.py` 分级普查（50k→100k→200k→全文件，不足才升级，断点续跑）
- **数据审计**（`output/cstnet60_20260918_exp/data_audit.json`）：118类/148文件/6.23GB 全部 pcap 大端、0异常、无符号链接；上游证据=cstnet_merge.log（45,846原始单会话pcap逐类合并）+数据集说明.md（_pNNN为流哈希分片非时间序；每会话=独立原始采集）
- **普查**：118类全部≥100 qualified（最少124/最多889）；114类停在50k、4类100k、无需200k；总耗时100.8s；逐类qualified/深度/读满落档
- **选类采样**（manifest_60c.jsonl）：60类×100=6000（train3600/val1200/test1200）；20多文件类+40单文件类按qualified降序；split_mode: capture_group 4 / test_only 13 / **session_level 43（明示会话级规模实验）**；固定seed确定性采样、隔离断言过
- **64包×64维主配置验证（val 1200）**：Acc .8475 / P .8563 / R .8475 / maxFPR .0110 —— **四项未达**；对照 64包Top96 .8517、128包Top64 .8567 均平台在~85%：瓶颈=类间可分性非包数/维数；**64+64在60类规模不够用**
- **一次封存test（1200）**（冻结=validation最优128pkt+Top64，预声明规则）：Acc .8375 / P .8472 / R .8375 / maxFPR .0161 —— 未达；类别级：≥.95 recall 18/60；按split_mode recall：capture_group .925 > session_level .844 > **test_only .789（测试采集组未见平均-6点=真实泛化缺口）**；混淆聚簇（acm↔researchgate、snapchat↔facebook、163↔eastmoney）；六类原班在60类语境退化（qq/acm 0.60 vs 六类实验0.95+）=类互扰实锤
- **特征账本**（caer_accounting_60c.json）：A=64冻结维，**E=63/64=98.4%（输出有效率≥80%达标）**，R=64/64；人工修正可用率≥90% 仍 BLOCKED_DATA
- 宿主遗留 `output/cstnet60_20260918/`（root只读）原样未动，本轮产物在 `output/cstnet60_20260918_exp/`
- 测试：新增 `tests/regression/test_cstnet_catalog_20260918.py`（15用例：发现/排序/异常/审计/六类兼容/划分纪律/确定性）；**全套168 passed**；test_all 6/6
- 详见 `docs/实施结果/扩类60_数据审计与64包64维验证_20260918.md`

### 下一步工作计划
- 60类识别缺口（Acc/R ~0.84 距 0.95/0.98）方向：SNI/证书强判别特征入候选池、混淆簇归并或类层次策略、每类观测扩到150+、剔除不可分簇后重配类集合
- 正式DPI规则链在60类上的 E 口径复算（本轮98.4%是XGBoost直达口径）
- 人工修正记录机制（可用率≥90%验证前提）仍缺

---

## 2026-09-19：多数据集实验探索与 MIRAGE-AppAct 冻结

### 本轮完成

- ✅ MIRAGE-AppAct fresh 多应用行为实验：Skype / Discord / Zoom / Teams，JSON activity capture 文件级隔离。
- ✅ 完成表示预算：prefix32 / 64 / 128 / full activity context。
- ✅ 完成 flow-context：4 / 8 / 16 / 32 / all flows。
- ✅ 完成维度：Top16 / 32 / 48 / 64 / 96 / 128。
- ✅ 完成 XGBoost / ExtraTrees / RandomForest / ensemble / hierarchical / app-conditioned 对照。
- ✅ 最终 DEV 冻结：Hybrid full-flow + prefix 候选，train-only Top48，XGBoost + ExtraTrees；Validation Acc=98.15%，Macro-R=98.09%，MaxFPR=2.74%，正式指标 PASS。
- ✅ 冻结配置后首次打开 fresh sealed Test 108 files：Acc=97.22%，Macro-P=97.60%，Macro-R=96.75%，MaxFPR=3.13%，3 errors；因 Recall<98%，正式 FAIL。Test 后未再调参。
- ✅ 关键旧 worktree 实验证据回收到 output/experiment_archive_20260919/，含 CESNET-QUIC22、USTC、CSTNET60、ISCXVPN/ISCXTor 与负结果。
- ✅ 根目录生成统一实验总账：华为杯_实验探索与最终裁决_20260919.md，并生成 SHA256。
- ✅ 修复 experiment_archive_20260919/SHA256SUMS，使其不包含自身。

### 当前固定技术裁决

- Application Profile：单 flow，30~64 packets/PPI，约 56~64 维。
- Behavior Profile：ISCXVPN 使用 15 s 时间窗口；MIRAGE 使用完整 activity-capture context，≤64维。
- Tunnel Profile：15 s 窗口 + 3-window temporal voting（约45 s），≤64维。
- CESNET-QUIC22 W47 sealed Test：Acc=98.375%，P=98.384%，R=98.375%，MaxFPR=0.529%，正式 PASS。
- ISCXVPN VPN chat/audio/video 15 s ExtraTrees：Acc≈99.39%，P≈99.17%，R≈99.15%，MaxFPR≈0.73%，正式 PASS。
- ISCXTor grouped Hybrid Top64 + 3-window：平均 Acc≈98.29%，P≈98.27%，R≈98.18%，MaxFPR≈3.05%；仍需 fresh 单一 holdout。
- MIRAGE fresh multi-app sealed Test：Acc=97.22%，R=96.75%，作为严格行为泛化压力结果，不再针对该 Test 调参。

### 后续只保留

1. 将 Application / Behavior / Tunnel 三 Profile 接回 mine → RuleBundle → DPI → evaluate 全链并验证规则/引擎指标。
2. 为 ISCXTor 构建 fresh grouped holdout，重点覆盖 Skype file-transfer / encrypted bulk traffic。
3. 建立 CrossPlatform Android↔iOS 经人工/元数据验证的 App mapping。
4. 对 CESNET corrected56 做最终人工确认，正式落档“人工修正后可用率≥90%”。
5. 补齐各 Profile 的吞吐、RSS 与时延表。


---

## 2026-09-19：三Profile RuleBundle / DPI 最终验收

### 已完成

- ✅ Application64：真实USTC PCAP→RuleBundle→独立rules-only→800 sealed Test，全链 Acc=99.75%、P=99.75%、R=99.75%、MaxFPR=0.17%，PASS。
- ✅ Tunnel15：ISCXTor 15s flow windows + 3-window vote→独立RuleBundle，file-disjoint Validation Acc=99.14%、P=99.12%、R=98.51%、MaxFPR=2.70%，PASS；仍需fresh grouped holdout。
- ⚠️ Behavior15：ISCXVPN原始PCAP 15s flow-window RuleBundle，file-disjoint Validation Acc=84.75%、P=74.82%、R=87.43%、MaxFPR=12.10%，FAIL。官方ARFF 99%+仅保留为离线参考，不再冒充部署成绩。
- ✅ 三Profile训练/独立推理 parity 一致；Application 400条0 mismatch，Behavior 767条0 mismatch，Tunnel parity=true。
- ✅ 新增统一验收归档 output/three_profile_acceptance_20260919/：三个profile的bundle/manifest/metrics/fidelity/performance + summary/status/reproduce/SHA256。
- ✅ tests/regression/test_profile_runtime_20260919.py: 7 passed；全部 regression: 126 passed；test_all.py: 6/6。

### 当前唯一主要识别阻塞

Behavior15 的原始PCAP file-disjoint泛化。下一阶段只能在train/validation侧改进Observation、标签过滤和行为上下文表达，不使用已打开sealed test做调参。


### Behavior15 追加诊断（2026-09-19）

- 时序投票仅使用既有file-disjoint Validation：3窗R≈94.20%、5窗R≈94.63%，均未达98%；7/9窗下降。
- 主流/Top-N过滤：最好byteshare≥10% + XGBoost，Acc≈86.68%、R≈90.36%、FPR≈21.58%，未解决问题。
- 裁决：Behavior剩余瓶颈主要是跨capture/跨应用形态变化，而非单纯背景流或时序抖动；停止在同一Validation继续试参。

---

## 2026-09-20：QUIC / TCP会话鲁棒性 / Behavior宽松口径 / Tor补全

### QUIC协议处理

- ✅ 真实VisQUIC专项：30个PCAP、12,312个UDP/QUIC-like包。
- ✅ 识别68个QUIC v1 Initial、12,047个Short Header，parse error=0。
- ✅ QUIC fixture覆盖v1 Initial、unknown long-header、short-header降级、普通UDP反例、截断安全，5/5 PASS。
- ✅ 协议处理约27,811 packets/s，Peak RSS约46.3 MiB。

### TCP乱序 / 重传 / 会话重建

- ✅ 9项状态机语义回归全部PASS：乱序、gap flush、重传去重、纯ACK、FIN、RST、TCP/UDP timeout、flush_all。
- ✅ 4个真实PCAP共325,000包、111,632会话、13次真实重传，parse error=0。
- ✅ 额外真实扫描：CSTNET acm两文件各1次乱序，ISCXVPN vpn_bittorrent前30k包检测2,951次乱序；三文件共2,953次。
- ✅ TCP/session处理约6,903 packets/s，Peak RSS约238.1 MiB。

### Behavior宽松口径

- ⚠️ session-disjoint raw-PCAP Test：Acc≈84.57%、P≈84.33%、R≈80.75%、FPR≈15.22%，FAIL。
- ⚠️ window-stratified raw-PCAP Test：Acc≈95.31%、P≈93.08%、R≈95.02%、FPR≈4.37%，FAIL。
- ✅ 官方15s ARFF feature-vector RuleBundle Test：Acc≈98.86%、P≈98.01%、R≈98.86%、FPR≈0.73%，PASS。
- 口径固定：官方ARFF结果只作为行为功能/规则表达能力证据，不替代raw-PCAP跨session/cross-capture泛化。

### Tor困难业务对

- ✅ 原Tunnel15普通file-disjoint Validation仍为R≈98.51%、FPR≈2.70%，PASS。
- ⚠️ post-exploration matched-pair hard stress：总体Acc≈91.39%、P≈94.83%、R≈83.02%、FPR≈33.96%，FAIL。
- Facebook Chat / SFTP / Skype Audio均100%；Vimeo Tor Recall≈33.33%；Skype file transfer Tor Recall≈5.88%。
- 裁决：SFTP已由histogram特征修复，Skype file transfer仍是主要未解决匿名流量子域。

### 正式工程表与测试

- ✅ 统一吞吐/RSS/端到端时延表：output/protocol_and_system_acceptance_20260920/performance/engineering_table.csv/json/md。
- ✅ 新增专项回归2 passed；全部regression 128 passed；test_all 6/6。
- ✅ 统一归档：output/protocol_and_system_acceptance_20260920/，含summary/status/reproduce/SHA256。
- ✅ 根目录总账追加第27节。

### 当前仍保留

1. Behavior raw-PCAP泛化未达98% Recall。
2. Tor Skype file-transfer hard pair未解决。
3. CrossPlatform Android/iOS mapping与实验未完成。
4. 人工修正后可用率待人工最终确认。
5. 按当前指令暂不进行release封版。

---

## 2026-09-20：多数据集混合特征选择诊断

- ✅ 使用USTC / CSTNET / CrossPlatform-China-Android / VisQUIC四个数据集，统一共享runtime前64包重新提取。
- ✅ 每数据集4类×80样本，总计1,280条；PARROT因当前runtime无法形成有效session，未强行混入，改用兼容的CrossPlatform。
- ✅ 单数据集Top64两两Jaccard约0.24–0.42；四数据集Top64全交集仅9维，证明关键指纹存在明显数据集差异。
- ✅ common_shape Consensus Top64中61/64至少被两个数据集共同选中；Specific→Consensus平均Accuracy 94.14%→92.19%，下降约1.95 pp。
- ✅ 普通16-class pooled Top64平均仅下降约0.78 pp，说明普通pool仍保留数据集特异/交互边界，不等价于交集特征。
- ✅ 固定每类40样本时，1→4数据集Accuracy约89.06%→88.54%，无单调下降。
- ✅ 固定总预算约320样本时，1→4数据集Accuracy 89.32%→77.08%，下降12.24 pp；该下降同时包含共享特征约束、类别数增加和单类样本稀释。
- 裁决：原“多数据集混合迫使选择共有特征并掉精度”假设部分成立；若要真正逼出泛化特征，应显式做dataset-wise importance/stability或worst-dataset约束，而非简单pool Top-K。
- 产物：output/mixed_dataset_feature_experiment_20260920/summary.md、results_common_shape.json、pooled_mix_common_shape.csv、SHA256SUMS。

---

## 2026-09-21：多数据集共享特征精度恢复

- ✅ 在不重新引入端口/TLS/QUIC/protocol/TCP-window等显式数据集指纹的前提下，继续使用common_shape和64维预算。
- ✅ 固定320条训练预算同split：equal20/class + consensus64 + XGBoost Test Acc=80.86%；robust Hybrid64 + difficulty-aware sampling + XGB/Extra Test Acc=85.94%，**+5.08 pp**。
- ✅ 最优320训练分配：CSTNET 36/class、CrossPlatform 24/class、USTC 12/class、VisQUIC 8/class；CrossPlatform相对基线+14.06 pp。
- ✅ 80维探索Test=85.16%，未超过64维85.94%，继续支持64维预算。
- ✅ Latent domain gate只看shared shape，在独立复现实验Test上域识别99.69%；Hard-routing Test Acc=90.94%。但同split预先候选flat ExtraTrees事后对照91.25%，因此不建议为了0.x pp引入MoE复杂度。
- ⚠️ 共享核心+域adapter：Validation最高91.88%，冻结Test仅87.19%，对CrossPlatform split敏感，不作为最终推荐。
- ✅ 固定robust shared64 + ExtraTrees + 40 train/class做5-seed重复：Mean Acc=89.38%±1.47 pp，Max=91.25%；CrossPlatform仍是主要方差源。
- ✅ 固定方法训练预算曲线：20/class=88.25%，30/class=88.33%，40/class=**90.58%**，50/class=**91.50%**；最高单次50/class=**93.75%**。
- 最终建议：**robust shared64 + ExtraTrees**；极小预算用difficulty-aware allocation；实际多数据集混合建议至少40 train/class。
- 产物：output/mixed_dataset_feature_recovery_20260921/summary.md、status.json、test_results.json、domain_routing/、shared_core_adapter/、repeated_fixed/、budget_curve/、SHA256SUMS。
- ✅ 根目录总账追加第29节。

---

## 2026-09-21（续）：四指标正式冲刺 · 泄漏修复 + 诚实 FAIL

### 本轮完成

- ⚠️ **发现并修复 v1 fresh split 的 capture 泄漏**：multi-file 类 held-out 文件的剩余 observation 曾进入 Train/Validation；修复后 held 文件全部行从 dev 剔除、Test 下采样、余量 drop；新增 `split_leakage_audit.json` 硬断言（三 split observation_id 两两交集=0、multi-file 类 dev/test source_file 交集=0），泄漏版产物隔离至 `leaked_v1_quarantine/`。
- ✅ 修复版 fresh split（seed=20260922，搜索前生成）：train 3,479 / val 720 / test 903（**未打开**）；CrossPlatform 训练池扩至全量会话；重新抽取 16 类 cache（prefix=64，含 197 维新通用序列族：位置签名/转移矩阵/双阈值 burst/自相关/累计曲线/时间窗节奏/尺寸多样性）。
- ✅ 正式搜索（15 子集 × 10 模型 = 150 配置 + top-3 三种子稳定性）：最好 **seq_full_pool(197维)+ens_xe：Validation Acc 96.11% / Macro-P 96.45% / Macro-R 95.21% / maxFPR 1.36%**；维度放宽代价单列（64维 94.9% → 197维 95.2% Macro-R，非阻塞项）。
- ❌ **裁决 FAIL（Macro Recall 95.2% vs 98%）**：按协议 frozen_config 未写、fresh Test 保持密封。证据链：1-NN 错误邻居仅 32% 同类；Train 内 LOO 1-NN CrossPlatform 77.2%；错误真标签 rank2=14/rank3=6/rank4+=7、概率差中位 0.454 → 完美决策规则上限 ~97.3%；aikan 仅 48 train；bagging/子空间/kNN/OOF 校准/过采样/窗口增广/MoE/cadence 全部实测无增益。
- ✅ 产物：summary/status/fresh_split_spec/split_leakage_audit/validation_leaderboard/validation_stability/search_outcome/reproduce/SHA256SUMS（无 frozen/test_metrics/per_class——Test 未打开，协议要求）。
- ✅ 根目录总账追加第 30 节。

### 下一步候选

1. 若纪律允许：SNI/证书长度族握手特征是最大判别源（当前明令禁止）。
2. CrossPlatform 数据侧根治：带服务标注采集或 App↔SDK 流清洗。
3. 观察单位升级为短时窗多会话聚合（行为层语义）。

---

## 2026-09-22：全场景四指标冲刺（观察单位升级 → Behavior session-disjoint PASS、Tor hard-pair 修复）

- 任务契约：允许 competition-oriented / identifier-assisted 轨道（端口/TLS/QUIC 可见字段、远端 IP feed），但必须与 shared-shape 泛化口径分开标注；sealed Test 只能开一次；做不到的严格场景保留 FAIL。统一归档 `output/all_scenarios_metric_push_20260922/`（各场景 README/协议、leaderboard、frozen_config（仅过门）、test_metrics、per_class、reproduce、SHA256SUMS；总表 summary_table.md）。
- ✅ **B behavior raw-PCAP 多流聚合窗 session-disjoint fresh Test 四项 PASS**：98.55/97.78/**99.31**/1.82（w45 聚合窗 top32+xgb；三种粒度 split 建模前预注册 seed 20260922）。旧单流同口径 FAIL 于 80.75/15.22——阻塞项是观察单位不是会话泛化。
- ⚠️ B window-stratified fresh Test：99.08/98.92/96.30/1.27 FAIL（9 个 video 窗 1 错，协议禁止重试）；capture-disjoint 57.3/77.5/66.9/84.2 FAIL 且比旧单流 file-disjoint 更差（聚合特征携带采集级背景指纹）——跨采集泛化仍未闭合。
- ✅ **C Tor hard-pair（post-exploration engineering stress）修复**：会话级聚合观察 142 测试会话 100/100/100/0，每对（含 Skype file-transfer、Vimeo）Tor recall 100%；因果投票 k=3/5/7 反而更差、阈值校准无效；普通 file-disjoint PASS 不动。
- ❌ A/A2 mixed16 fresh v2 两轨仍 FAIL（Test 保持密封）：握手可见字段轨最好 97.22/97.51/96.25/0.91；identifier-assisted IP-feed 轨最好 98.06/97.96/97.17/0.91（CSTNET 采集无 Client Hello，SNI 结构性不可用；IP-only 4 维已达 96.25）。Macro-R 停在 ~97.2，错误仍集中在 CrossPlatform 短会话。
- ❌ D/D2 MIRAGE activity 新 fresh 轮 FAIL（Test 未开）：merged-stream 96.49/96.17/96.17/2.38；hybrid（prefix+fullflow 重算 5 app）97.37/97.28/97.28/1.49——与 20260919 消耗掉的 sealed Test（R 96.75）互证 ~97.3 天花板。
- ❌ E CrossPlatform Android↔iOS 首轮正式映射（诚实 FAIL/非正式目标）：28 对产品名筛出 9 个双侧真实采集（其余为 stub/空文件，SKIP 留痕）；平台转移（=采集隔离）最好 38.6% Acc，pooled 控制组 89.1%——数据级跨 OS 迁移远低于阈值；若"跨平台"指工具跨 OS 运行则非正式硬指标，两种解读分开表述。
- 方法论结论：观察单位升级（多流聚合窗/会话聚合）是本轮唯一真正翻盘的杠杆；决策层手段（投票/阈值/类偏置）在同一观察单位上无增益。
- 生产代码（src/）零改动；实验脚本 scripts/run_scenario_*_20260922.py 独立于回归。根目录总账追加第 31 节；详见 docs/实施结果/全场景指标冲刺_20260922.md。

---

## 2026-09-22（续）：最终实验收尾——全部未闭合场景闭账

任务契约：把第 31 节未闭合实验全部收账（能用现有数据跑完的实际跑完；无法
改善的完成最后一轮针对性有限搜索后给最终裁决），不留"继续试试"式待办；
sealed Test 纪律不变（门=单次四指标+3种子稳定性，未过不开）。统一归档
`output/final_experiment_closure_20260922/`（summary/summary_table/status/
reproduce/SHA256SUMS + 每场景子目录与根因文档）；根目录总账追加第 32 节；
详见 `docs/实施结果/最终实验收尾_20260922.md`。

### 最终状态总表

| 场景 | 最好真实指标 | Test | 最终状态 |
|---|---|---|---|
| B behavior capture-disjoint | 门最好 56.7/58.2/70.8/41.5（216配置全败）；LOCO(raw,17非测试捕获) 92.7% | 未开（永久密封） | FINAL_FAIL（数据侧：标签构成缺陷捕获 facebook_audio2 实为 chat 形态 2.96pkt/s；捕获相对化 capz/caprank 被证伪） |
| A2+ mixed16 端点池化 | 单次 98.89/98.88/98.48/0.30 全过；3种子均值 R 97.80 | 未开（永久密封） | FINAL_FAIL（四轨 95.2→96.3→97.17→98.48单次/97.80稳定 证据链闭合） |
| D3 MIRAGE 短/长流分组 | 97.37/97.28/97.28/1.49；与 D2 完全同 3 错（全 Zoom） | 未开（永久密封） | FINAL_FAIL（三表征+旧sealed互证 ~97.3 天花板） |
| E CrossPlatform 最终对齐 | 会话级 CORAL 44.4%（+5.8pp）；capture聚合 66.7%(n=9)；IP feed 有效上限≈46% | — | FINAL_FAIL（结构性：每(app,platform)仅1采集） |
| F 人工修正可用率≥90% | 机器侧就绪：184 行审阅表×3 候选包 + compute_usable_rate.py（无人工记录拒出数） | — | EXTERNAL_REVIEW（仅剩人工签字） |

### 本轮关键发现

- **端点池化有效但不稳**：capture 内同远端 /24 会话概率池化（identifier-
  assisted，无标签不跨文件）把 mixed16 验证 R 从 97.17 推到 98.48（单次），
  但 3 种子均值 97.80——98% 线在当前数据噪声带内，Test 不开。
- **捕获内相对化双语料证伪**：capz/caprank 在 behavior 与 CrossPlatform
  上都大幅劣于 raw——绝对尺度是可迁移信号，该方向不再搜索。
- 三个 FAIL 的根因均为数据条件（标签构成缺陷 / 单采集类互穿 / 每
  (app,platform)单采集），非模型选择问题。
- 人工可用率：全项目无人工修正记录（不伪造）；机器侧审阅包完成，
  只剩人工逐行签字。

### 实验待办总账（终态）

- **已完成（PASS）**：CESNET-QUIC22、ISCXVPN 官方ARFF、Application64、
  Tunnel15 file-disjoint、Tor hard-pair 会话聚合、behavior 多流聚合窗
  session-disjoint、CSTNET60 E=63/64。
- **FINAL_FAIL（不再保留待办）**：behavior capture-disjoint、mixed16
  fresh v2（四轨）、MIRAGE fresh grouped（三表征）、CrossPlatform
  Android↔iOS（结构性）。
- **外部依赖（非实验待办）**：人工修正可用率签字（manual_review_readiness/）；
  数据侧恢复条件（多采集/服务标注/窗口级真值）如后继有新数据再开新 split。

### 测试

- 新增 `tests/regression/test_final_closure_20260922.py`（7 用例：端点池化
  分组不跨文件/无标签/确定性、capz/caprank 捕获内隔离）；生产 src/ 零改动。
